// Prevent us from attempting to use variables
// that are not declared
"use strict"


/* 
// declaration of constant variables for a cat cafe
const cafeName = "WhiskerWonderland";
const state = "Colorado";
const city = "Aspen";

//attempt to reassign constant variable
// city = "louisville";
//TypeError: Assignment to constant variable.

// declaration of let variables for resident cat info

let adoptionStatus = false;
let healthCondition = "fair" 
let age = 2;
console.log(adoptionStatus);
console.log(healthCondition);
console.log(age);
 */
